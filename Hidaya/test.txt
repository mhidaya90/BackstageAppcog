<Header
        title={
          <Box display="flex" alignItems="center">
            <Button
              startIcon={<ArrowBackIcon />}
              onClick={() => navigate(-1)}
              color="primary"
              style={{ marginRight: 16 }}
            >
              Back
            </Button>
            <Typography variant="h5">Bitrise Builds</Typography>
          </Box>
        }
        subtitle="Latest build status from Bitrise"
      />
