import { Router } from 'express';

export async function createRouter(): Promise<Router> {
  const router = Router();

  router.get('/hello', (_, res) => {
    res.json({ message: 'Hello from my backend plugin!' });
  });

  return router;
}




import { createRouter as myPluginRouter } from '../plugins/my-plugin/src/service/router';

async function main() {
  // ...
  const myPlugin = await myPluginRouter();
  apiRouter.use('/my-plugin', myPlugin);
  // ...
}
