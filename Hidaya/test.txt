
const fetchBuildDetails = async (buildId: string) => {
  const fetchApi = useApi(fetchApiRef);
  const response = await fetchApi.fetch(`/api/bitrise/build/${buildId}`);
  if (!response.ok) {
    throw new Error(`Failed to fetch build: ${response.statusText}`);
  }
  return await response.json();
};
