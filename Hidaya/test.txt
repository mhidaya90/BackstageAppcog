
export interface BitriseService {
  apiToken: string;
  getBuilds(appSlug: string): Promise<any>; // Adjust return type as needed
  triggerBuild(appSlug: string, workflowId: string): Promise<any>; // Optional
}
