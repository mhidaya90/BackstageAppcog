
 FAIL   @backstage/plugin-flowsource-bitrise-backend  plugins/flowsource-bitrise-backend/src/plugin.test.ts
  ● flowsourceBitrisePlugin › should register and initialize the plugin correctly

    expect(jest.fn()).toHaveBeenCalled()

    Expected number of calls: >= 1
    Received number of calls:    0

      41 |     //plugin.register(env);
      42 |
    > 43 |     expect(registerInitMock).toHaveBeenCalled();
         |                              ^
      44 |     const initConfig = registerInitMock.mock.calls[0][0];
      45 |     expect(initConfig.deps).toHaveProperty('logger');
      46 |     expect(initConfig.deps).toHaveProperty('httpRouter');

      at Object.toHaveBeenCalled (plugin.test.ts:43:30)

 FAIL   @backstage/plugin-flowsource-bitrise-backend  plugins/flowsource-bitrise-backend/src/router.test.ts
  ● Router › GET /bitrise/build/:appSlug should return build info

    expect(received).toBe(expected) // Object.is equality

    Expected: 200
    Received: 404
